CREATE TYPE task_status AS ENUM ('todo', 'in_progress', 'done'); CREATE TABLE tasks (id uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL, title text NOT NULL, description text, project_id uuid NOT NULL REFERENCES projects(id), assignee_id uuid REFERENCES users(id), status task_status NOT NULL, created_at timestamp DEFAULT now() NOT NULL);
